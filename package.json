{
  "name": "plain",
  "version": "1.2.0",
  "description": "A Git-native CMS: the repository is the database, static files are the API, and AI is the admin.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "test": "node --test tests/",
    "manifest": "node tools/engine-manifest.js"
  },
  "dependencies": {
    "marked": "^15.0.0"
  }
}
